# 文档格式与命名规范

## 文档信息

| 字段     | 内容                       |
| -------- | -------------------------- |
| 版本     | 1.0.0                      |
| 日期     | 2026-09-04                 |
| 状态     | Draft                      |
| 适用范围 | 全部产品文档（PRD / NFR / 架构 / API / 发布 / UI），不含代码与数据模型 |

## 修订历史

| 版本 | 日期       | 作者 | 变更说明 |
| ---- | ---------- | ---- | -------- |
| 1.0.0 | 2026-09-04 |      | 初稿：统一文件命名、文档编号、文档头、交叉引用、需求 ID、术语 |

---

## 0. 本规范解决的问题

现状存在三套并行且互相冲突的命名方案，导致交叉引用断链、同一文档三个叫法：

| 冲突写法 | 出现位置 | 处置 |
|---|---|---|
| `[product]-prd.md` / `[product]-modules.md` / `[product]-details.md` | `ref-0000-模板系统说明.md`「最佳实践 1」、`tmp-prd-0000` 参考文档节 | **废弃** |
| `PRD-[产品名称]-Overview.md` / `-Modules.md` | `tmp-prd-0002` 文档头「关联文档」 | **废弃** |
| `prd-[4位编号]-[项目名]-[module]-[submodule].md` | `ref-0004-PRD仓库目录结构方案.md` | **采纳为唯一标准** |

以下全部按采纳方案定义。

---

## 1. 文件命名

### 1.1 三类前缀

| 前缀 | 用途 | 谁来写 |
|---|---|---|
| `tmp-` | 模板（template），供复制 | 规范维护者 |
| `ref-` | 规范、指南、参考卡 | 规范维护者 |
| 业务前缀 | 实例文档，由模板复制产生 | 文档作者 |

> `tmp` = template，不是 temporary。模板文件永不直接填写，必须先复制。

### 1.2 模板与规范文件

```text
tmp-[类型]-[4位序号]-[中文名]模板.md
ref-[4位序号]-[中文名].md
```

类型枚举（与实例文档前缀一一对应）：`prd` `nfr` `arc` `api` `db` `rel` `ui`

示例：

```text
tmp-prd-0000-产品概述模板.md
tmp-prd-0001-模块设计模板.md
tmp-nfr-0000-非功能需求模板.md
ref-0004-PRD仓库目录结构方案.md
ref-0005-文档格式与命名规范.md
```

规则：
- 序号 4 位，同类型内递增，不复用已删除的序号
- 中文名不含空格、不含 `/`、不加版本号
- 模板文件名必须以「模板」二字结尾，实例文档不得带「模板」

### 1.3 实例文档

```text
[类型]-[4位序号]-[项目名]-[module]-[submodule].md
```

示例：

```text
prd-0001-myapp-product-overview.md
prd-0014-myapp-pay-payment-processing.md
nfr-0002-myapp-user-account-auth.md
```

规则：
- **序号全仓库唯一、递增，不按模块分段**
- 项目名、module、submodule 全部小写 kebab-case，与所在目录名逐级一致
- 无子模块时省略末段，不留空占位
- 一个产品的概述文档全仓库只有一份，追加需求写进对应模块设计或功能细节，不新建第二份概述

### 1.4 目录

```text
[module]/[submodule]/[文件]
```

目录名英文 kebab-case，与文件名中的 module / submodule 段完全一致。

---

## 2. 文档编号

**文件名即文档编号，不设第二套编号。**

删除文档头里 `PRD-[产品标识]-001` 一类的独立编号字段——它与文件名序号各自递增，必然漂移。需要在正文引用某文档时，写文件名或相对路径链接。

---

## 3. 文档头

每份文档开头必须依次包含「标题 → 文档信息 → 修订历史 → 分隔线」。

```markdown
# [产品名称] - [文档用途]

## 文档信息

| 字段     | 内容                    |
| -------- | ----------------------- |
| 版本     | 1.0.0                   |
| 日期     | YYYY-MM-DD              |
| 作者     | [作者名称]              |
| 状态     | Draft / Review / Approved |
| 关联文档 | [文档名](相对路径) \| [文档名](相对路径) |

## 修订历史

| 版本 | 日期       | 作者     | 变更说明 |
| ---- | ---------- | -------- | -------- |
| 1.0.0 | YYYY-MM-DD | [作者名] | 初稿     |

---
```

字段规则：
- 「关联文档」为**必填**，所有文档类型一致，不允许某些文档有、某些没有
- 「状态」取值仅三种：`Draft` / `Review` / `Approved`
- 日期一律 `YYYY-MM-DD`，不写「今天」「本周」等相对时间
- 版本号 `主.次.修订`（三段，如 `1.0.0`）：结构性改动进主版本，新增章节进次版本，勘误与措辞进修订号
- 每次改动必须新增一行修订历史，不允许覆盖已有行

文档结尾统一：

```markdown
---

**文档状态：** [状态]
**最后更新：** YYYY-MM-DD
**负责人：** [负责人名称]
```

「文档状态 / 最后更新」必须与文档头一致。

---

## 4. 章节与正文

- 一级标题 `#` 全文只有一个，即文档标题
- 章节用带序号的 `##`（`## 1. xxx`），子节 `### 1.1 xxx`，最深到 `####`
- 章节序号连续，删除章节后重新编号
- 表格必须有表头；列宽不要求对齐
- 代码块必须标注语言（`text` / `gherkin` / `json` / `mermaid` …），纯图示用 `text`
- 占位符统一 `[中文说明]`，路径与标识符占位统一 `[project]` / `[module]` / `[submodule]`，不混用 `[产品标识]` `[product]` 两种写法

---

## 5. 交叉引用

### 5.1 链接

- 一律使用**相对路径**，不写绝对路径、不写仓库 URL
- 链接文字写文档用途，不写文件名：`[模块设计](../pay/prd-0014-myapp-pay-payment-processing.md)`
- 引用模板时链接模板真实文件名，不得使用别名

### 5.2 锚点

**跨文档锚点必须带文件路径**：

```markdown
✅ [模块 1](./prd-0002-myapp-module-design.md#1-模块1名称)
❌ [模块 1](#1-模块1名称)     ← 会落在本文件内，链接失效
```

### 5.3 改名与移动

改文件名或移动目录时，必须同步搜索并修正全部指向它的链接。文件名一经发布，非必要不改。

---

## 6. 需求 ID

| 层级 | 格式 | 示例 |
|---|---|---|
| 模块 | `MOD-[2位]` | `MOD-01` |
| 子模块 | `MOD-[2位]-SUB-[2位]` | `MOD-01-SUB-03` |
| 需求 | `fun-[module]-[submodule]-[4位]` | `fun-user-auth-0001` |

规则：
- `module` / `submodule` 段与目录名、文件名中的对应段完全一致
- ID 一经分配永不复用、永不改写；需求作废只改状态
- 全仓库唯一权威索引为 `prd-0000-[项目名]-requirement-index.md`，新增需求先在索引登记再写正文
- 代码中引用写 `@req fun-user-auth-0001` / `@submodule MOD-01-SUB-01` / `@module MOD-01`

---

## 7. 术语统一

同一概念全部文档只用一个词。

| 统一用词 | 禁止的同义写法 |
|---|---|
| 验收标准 | 接受度标准、Acceptance Criteria、AC |
| 功能细节 | 详细细节、详细功能细节 |
| 模块设计 | 模块级别设计、模块概览 |
| 产品概述 | 产品总览、Overview |
| 非功能需求 | NFR 需求、质量属性 |
| 缺陷 / Bug | Issue、问题单 |

新增术语必须先进产品概述的「术语表」，再在其他文档使用。

---

## 8. 取值枚举

| 维度 | 取值 | 说明 |
|---|---|---|
| 优先级 | `P0` / `P1` / `P2` | P0 必需，P1 重要，P2 可选 |
| 文档状态 | `Draft` / `Review` / `Approved` | |
| 需求状态 | `TODO` / `IN_PROGRESS` / `DONE` / `SKIP` | 与需求索引一致 |

不得在文档中引入表外取值（如 `P3`、`待定`、`Pending`）。

---

## 9. 提交前检查清单

- [ ] 文件名符合 §1，序号全仓库唯一
- [ ] 文件名不含「模板」二字（模板文件除外）
- [ ] 文档头四要素齐全，「关联文档」已填
- [ ] 本次改动已加修订历史，版本号已升
- [ ] 文末「文档状态 / 最后更新」与文档头一致
- [ ] 全部链接为相对路径，逐条点击可达
- [ ] 跨文档锚点带文件路径
- [ ] 需求 ID 已在需求索引登记，module / submodule 段与路径一致
- [ ] 术语、优先级、状态取值符合 §7 §8
- [ ] 文档内无第二套文档编号
